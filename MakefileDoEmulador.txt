KBUILD_CFLAGS += -w
obj-m += Emulador.o
Emulador-objs := ./src/emulator/main.o ./src/emulator/vmachine.o

all:
	make -w -C /lib/modules/$(shell uname -r)/build M=$(PWD) modules

clean:
	make -C /lib/modules/$(shell uname -r)/build M=$(PWD) clean